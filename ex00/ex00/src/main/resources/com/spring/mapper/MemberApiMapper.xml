<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.spring.mapper.MemberApiMapper"> <!--  -->
	  <select id="selectMemberList" resultType="com.spring.domain.MemberDTO">
	      SELECT * FROM member
	  </select>
	  
	  <select id="selectMemberCnt" resultType="int">
	      SELECT COUNT(*) AS result FROM member WHERE id=#{id} AND pwd=#{pwd}
	  </select>
	  
	  <delete id="deleteMember">
	      DELETE FROM member WHERE m_no=#{m_no}
	  </delete>
	  
	  <insert id="insertMember">
	  	  INSERT INTO member(m_no, name, jumin, id, pwd) VALUES(seq_member_mno.nextval,#{name},#{jumin}, #{id},#{pwd})	  
	  </insert>
	  
	  <update id="updateMemberName">
	  	  UPDATE member SET NAME=#{name} WHERE M_NO=#{m_no}
	  </update>
  </mapper>